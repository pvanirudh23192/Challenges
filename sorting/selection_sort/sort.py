class SelectionSort:
    def __init__(self, arr):
        self.list_to_be_sorted = arr
        self.len = length(arr)

    def sort():
        arr = self.list_to_be_sorted
        for element in range(self.len):
            
